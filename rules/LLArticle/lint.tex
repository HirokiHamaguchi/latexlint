\section{LLArticle}

A $n$-dim. An $n$-dim. a $n$-dim. an $n$-dim.
A $n$ dim. An $n$ dim. a $n$ dim. an $n$ dim.

\subsection{Single Letters}

OK: an $a$-dim, a $b$-dim. a $u$-dim.

\begin{itemize}
	\item NG: `a $a$` → OK: `an $a$`
	\item NG: `a $e$` → OK: `an $e$`
	\item NG: `a $f$` → OK: `an $f$`
	\item NG: `a $h$` → OK: `an $h$`
	\item NG: `a $i$` → OK: `an $i$`
	\item NG: `a $l$` → OK: `an $l$`
	\item NG: `a $m$` → OK: `an $m$`
	\item NG: `a $n$` → OK: `an $n$`
	\item NG: `a $o$` → OK: `an $o$`
	\item NG: `a $r$` → OK: `an $r$`
	\item NG: `a $s$` → OK: `an $s$`
	\item NG: `a $x$` → OK: `an $x$`
	\item NG: `an $u$` → OK: `a $u$`
\end{itemize}

\subsection{Abbreviations}

\begin{itemize}
	\item NG: `a EM` → OK: `an EM` (Expectation--Maximization)
	\item NG: `a EVD` → OK: `an EVD` (Eigenvalue Decomposition)
	\item NG: `a FFT` → OK: `an FFT` (Fast Fourier Transform)
	\item NG: `a NP-hard` → OK: `an NP-hard` (Non-deterministic Polynomial-time hard)
	\item NG: `a LSTM` → OK: `an LSTM` (Long Short-Term Memory)
	\item NG: `a LTI` → OK: `an LTI` (Linear Time-Invariant)
	\item NG: `a MLE` → OK: `an MLE` (Maximum Likelihood Estimation)
	\item NG: `a MSE` → OK: `an MSE` (Mean Squared Error)
	\item NG: `a ODE` → OK: `an ODE` (Ordinary Differential Equation)
	\item NG: `a RNN` → OK: `an RNN` (Recurrent Neural Network)
	\item NG: `a RKHS` → OK: `an RKHS` (Reproducing Kernel Hilbert Space)
	\item NG: `a SDE` → OK: `an SDE` (Stochastic Differential Equation)
	\item NG: `a SVD` → OK: `an SVD` (Singular Value Decomposition)
	\item NG: `a SVM` → OK: `an SVM` (Support Vector Machine)
	\item NG: `a XOR` → OK: `an XOR` (Exclusive OR)
\end{itemize}

\subsection{LaTeX Commands}

\begin{itemize}
	\item NG: `a $\mathbb{R}$-valued function` → OK: `an $\mathbb{R}$-valued function`
	\item NG: `a $L^1$ norm` → OK: `an $L^1$ norm`
	\item NG: `a $L^2$ norm` → OK: `an $L^2$ norm`
	\item NG: `a $L^\infty$ norm` → OK: `an $L^\infty$ norm`
	\item NG: `a $\ell^1$ norm` → OK: `an $\ell^1$ norm`
	\item NG: `a $\ell^2$ norm` → OK: `an $\ell^2$ norm`
	\item NG: `a $\ell^\infty$ norm` → OK: `an $\ell^\infty$ norm`
	\item NG: `a $\mathcal{L}^1$ norm` → OK: `an $\mathcal{L}^1$ norm`
	\item NG: `a $\mathcal{L}^2$ norm` → OK: `an $\mathcal{L}^2$ norm`
	\item NG: `a $\mathcal{L}^\infty$ norm` → OK: `an $\mathcal{L}^\infty$ norm`
	\item NG: `a $x$-axis` → OK: `an $x$-axis`
\end{itemize}
