\section{LLSpaceEnglish}

Space between text and inline math like\(x=1\) and \(y=2\)lacks a space.
No spaces before commas, such as \(z=3\), we do not detect them.

We exclude cases like H\({}_2\)O and C\({}_{n}\)H\({}_{2n+2}\).
% todo: detect for the cases like $...$. This is quite hard.
