\section{LLErrCompOps}

\begin{gather*}
	x \leq y \quad
	x \geq y \quad
	x = y \quad
	\text{\tA{ok}} \\
	x <= y \quad
	x < = y \quad
	x =< y \quad
	x = < y \quad
	x \le = y \quad
	x = \le y \quad
	x \leq = y \quad
	x = \leq y \quad
	\text{\tD{ng}} \\
	x => y \quad
	x = > y \quad
	x >= y \quad
	x > = y \quad
	x \ge = y \quad
	x = \ge y \quad
	x \geq = y \quad
	x = \geq y \quad
	\text{\tD{ng}}
\end{gather*}
